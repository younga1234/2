---
name: 자동설정
description: 모든 것을 베스트 프랙티스로 자동 설정 (클릭 한 번!)
---

# ⚙️ 자동 설정 (One-Click Setup)

20년차 시니어 개발자의 완벽한 설정을 자동으로 적용합니다!

## 🚀 전체 자동 설정 실행

다음을 **모두 자동으로** 처리합니다:

```bash
echo "🎯 자동 설정을 시작합니다..."
echo "☕ 커피 한 잔 하세요! 5분이면 모든 게 준비됩니다."
echo ""

# 1. Git 자동 설정
echo "1/10 Git 설정 중..."
/agent auto-architect "Git 베스트 프랙티스 자동 설정"

# 2. 가상환경 자동 생성
echo "2/10 가상환경 생성 중..."
python -m venv .venv 2>/dev/null || python3 -m venv .venv

# 3. 의존성 자동 설치
echo "3/10 필요한 도구 설치 중..."
source .venv/bin/activate 2>/dev/null || . .venv/Scripts/activate
pip install --upgrade pip
[ -f requirements.txt ] && pip install -r requirements.txt

# 4. 환경변수 자동 설정
echo "4/10 환경 설정 중..."
/agent beginner-helper "환경변수 파일 자동 생성"

# 5. 데이터베이스 자동 초기화
echo "5/10 데이터베이스 준비 중..."
/agent database-expert "데이터베이스 자동 설정 및 마이그레이션"

# 6. 샘플 데이터 자동 추가
echo "6/10 샘플 데이터 추가 중..."
/agent project-wizard "테스트용 샘플 데이터 생성"

# 7. 테스트 환경 자동 설정
echo "7/10 테스트 환경 설정 중..."
/agent test-engineer "pytest 및 테스트 구조 자동 생성"

# 8. 코드 품질 도구 자동 설정
echo "8/10 코드 품질 도구 설정 중..."
pip install black flake8 mypy pytest-cov

# 9. Docker 설정 자동 생성
echo "9/10 Docker 설정 생성 중..."
/agent devops-engineer "Dockerfile 및 docker-compose 자동 생성"

# 10. 문서 자동 생성
echo "10/10 문서 생성 중..."
/agent documentation-writer "README 및 가이드 자동 생성"

echo ""
echo "✅ 완료! 모든 설정이 끝났어요!"
```

## 📋 자동 설정 항목 (상세)

### 1. Git 설정
```bash
# 자동으로 설정되는 것들
git config --local user.name "Your Name"  # 물어봐서 설정
git config --local user.email "your@email.com"

# .gitignore 자동 생성 (Python 최적화)
cat > .gitignore << EOF
# 환경변수 (절대 공유 금지!)
.env
.env.local

# 가상환경
.venv/
venv/
ENV/

# Python 캐시
__pycache__/
*.py[cod]
*$py.class

# 데이터베이스
*.db
*.sqlite3
db.sqlite3

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# 로그
*.log
logs/

# 비밀 파일
secrets/
*.pem
*.key
EOF

echo "✅ Git 설정 완료!"
```

### 2. 환경변수 자동 설정
```bash
# .env 파일 자동 생성
cat > .env << EOF
# 자동 생성된 환경변수
# 🔒 이 파일은 절대 Git에 올리지 마세요!

# 데이터베이스
DATABASE_URL=sqlite:///./app.db

# 보안
SECRET_KEY=$(python -c "import secrets; print(secrets.token_urlsafe(32))")

# 개발 모드
DEBUG=True
ENVIRONMENT=development

# API 설정
API_HOST=0.0.0.0
API_PORT=8000
EOF

# .env.example 템플릿 생성 (Git에 포함 가능)
cat > .env.example << EOF
# 환경변수 템플릿
DATABASE_URL=your_database_url_here
SECRET_KEY=your_secret_key_here
DEBUG=True
ENVIRONMENT=development
API_HOST=0.0.0.0
API_PORT=8000
EOF

echo "✅ 환경변수 설정 완료!"
echo "💡 .env 파일에 실제 값이 자동으로 입력되었어요"
```

### 3. 프로젝트 구조 자동 생성
```bash
# 시니어 개발자의 표준 폴더 구조
mkdir -p app/{api,models,services,utils}
mkdir -p tests/{unit,integration}
mkdir -p docs
mkdir -p scripts
mkdir -p data/{raw,processed}

# 각 폴더에 __init__.py 자동 생성
touch app/__init__.py
touch app/api/__init__.py
touch app/models/__init__.py
touch app/services/__init__.py
touch app/utils/__init__.py
touch tests/__init__.py

# 기본 파일들 자동 생성
/agent project-wizard "main.py, config.py 등 자동 생성"

echo "✅ 프로젝트 구조 생성 완료!"
```

### 4. 코드 품질 도구 자동 설정
```bash
# pyproject.toml 자동 생성 (모던한 Python 설정)
cat > pyproject.toml << EOF
[tool.black]
line-length = 88
target-version = ['py38', 'py39', 'py310', 'py311']
include = '\.pyi?$'

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
python_functions = "test_*"
addopts = "-v --cov=app --cov-report=html --cov-report=term-missing"
EOF

# pre-commit 훅 자동 설정
cat > .pre-commit-config.yaml << EOF
repos:
  - repo: https://github.com/psf/black
    rev: 23.1.0
    hooks:
      - id: black

  - repo: https://github.com/pycqa/flake8
    rev: 6.0.0
    hooks:
      - id: flake8

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.4.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
EOF

echo "✅ 코드 품질 도구 설정 완료!"
```

### 5. 데이터베이스 자동 설정
```bash
# SQLAlchemy 모델 베이스 자동 생성
cat > app/models/base.py << 'EOF'
from sqlalchemy import create_engine
from sqlalchemy.ext.declarative import declarative_base
from sqlalchemy.orm import sessionmaker
import os

DATABASE_URL = os.getenv("DATABASE_URL", "sqlite:///./app.db")

engine = create_engine(DATABASE_URL)
SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)
Base = declarative_base()

def get_db():
    """데이터베이스 세션 가져오기"""
    db = SessionLocal()
    try:
        yield db
    finally:
        db.close()
EOF

# 샘플 모델 자동 생성
/agent database-expert "User, Item 등 기본 모델 자동 생성"

echo "✅ 데이터베이스 설정 완료!"
```

### 6. Docker 자동 설정
```bash
# Dockerfile 자동 생성 (최적화된 멀티스테이지 빌드)
cat > Dockerfile << 'EOF'
# 🐳 자동 생성된 Dockerfile (베스트 프랙티스 적용)
FROM python:3.10-slim as base

WORKDIR /app

# 의존성만 먼저 복사 (캐시 최적화)
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 애플리케이션 코드 복사
COPY . .

# 비루트 사용자로 실행 (보안)
RUN useradd -m -u 1000 appuser && chown -R appuser:appuser /app
USER appuser

EXPOSE 8000

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
EOF

# docker-compose.yml 자동 생성
cat > docker-compose.yml << 'EOF'
version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql://user:pass@db:5432/dbname
    depends_on:
      - db
    volumes:
      - .:/app

  db:
    image: postgres:15
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=pass
      - POSTGRES_DB=dbname
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
EOF

echo "✅ Docker 설정 완료!"
```

## 🎉 설정 완료 후

모든 자동 설정이 끝나면 사용자에게:

```markdown
# 🎊 축하합니다! 설정이 모두 완료되었어요!

## ✅ 설정된 것들
- [x] Git 설정 및 .gitignore
- [x] Python 가상환경 (.venv/)
- [x] 모든 필요한 패키지 설치
- [x] 환경변수 (.env)
- [x] 데이터베이스 (SQLite)
- [x] 샘플 데이터 (10개 레코드)
- [x] 테스트 환경 (pytest)
- [x] 코드 품질 도구 (black, flake8, mypy)
- [x] Docker 설정
- [x] 문서 (README.md, 가이드)

## 🚀 지금 바로 실행해보세요!

### 방법 1: 직접 실행
```bash
source .venv/bin/activate  # 가상환경 활성화
python main.py             # 프로그램 실행
```

### 방법 2: Docker로 실행
```bash
docker-compose up          # Docker로 실행
```

### 방법 3: 개발 모드
```bash
uvicorn app.main:app --reload  # 자동 새로고침
```

## 🌐 접속 방법
- 브라우저에서 열기: http://localhost:8000
- API 문서 보기: http://localhost:8000/docs
- 관리자 페이지: http://localhost:8000/admin

## 📚 다음에 할 일
1. /무엇을할까 - 다음 단계 확인
2. /초보자모드 - 쉬운 개발 모드
3. 코드 수정 시작!

## 💡 도움말
문제가 생기면 언제든 물어보세요!
- "에러가 났어요" → 자동으로 해결
- "이게 뭐예요?" → 쉽게 설명
- "다음에 뭐 하죠?" → 자동 제안

---
⭐ 팁: 이 설정은 업계 표준(베스트 프랙티스)을 따릅니다.
   20년차 시니어 개발자와 똑같은 환경이에요!
```

## 🔍 문제 해결 (자동)

설정 중 문제가 발생하면:

```bash
# 자동으로 beginner-helper 호출
/agent beginner-helper "설정 중 발생한 에러 자동 수정"
```

예시:
```
🔧 문제 발견 및 자동 해결

❌ Python 3.10이 없어요
✅ Python 3.9로 자동 변경 (호환됨)

❌ PostgreSQL 설치 안 됨
✅ SQLite로 자동 전환 (더 간단해요!)

❌ Docker가 실행 중이 아님
✅ 직접 실행 방법으로 자동 변경

모든 문제가 해결되었어요! 😊
```
